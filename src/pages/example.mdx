---

---
import Layout from "../layouts/Layout.astro";
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import { SITE } from "../config/site.mjs";

<Layout
  title={`MDX Example | ${SITE.title}`}
  description="See MDX in action with Astro Base. Learn how to write JSX in your Markdown documents for blog posts, documentation, and content-rich pages."
  canonicalURL={`${SITE.url}/example`}
>
  <Header />
  
  <main id="main-content" role="main">
    <article class="prose prose-lg max-w-4xl mx-auto px-4 py-20">
      # MDX Example
      
      This page demonstrates MDX support in Astro. You can write Markdown with JSX components!

      ## What is MDX?

      MDX lets you write **JSX in your Markdown documents**. This makes it perfect for:

      - Writing blog posts
      - Creating documentation
      - Building content-rich pages

      ## Using Components in MDX

      You can import and use Astro components directly in your MDX files:

      ```jsx
      import MyComponent from '../components/MyComponent.astro';
      
      <MyComponent />
      ```

      ## Features

      1. **Markdown syntax** - Write content naturally
      2. **JSX support** - Use React/Astro components
      3. **Frontmatter** - Add metadata to your pages
      4. **TypeScript** - Full type safety

      ## Example Code Block

      ```javascript
      function greet(name) {
        return `Hello, ${name}!`;
      }
      ```

      > This is a blockquote example showing how MDX handles standard Markdown features.

      - List item one
      - List item two
      - List item three

    </article>
  </main>
  
  <Footer />
</Layout>

