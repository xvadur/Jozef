---
// Example component demonstrating Sass usage
---

<div class="sass-example">
  <div class="example-card">
    <h3 class="title">Sass Example</h3>
    <p class="description">
      This component demonstrates Sass features like variables, nesting, and mixins.
      The rest of the site continues to use Tailwind CSS for styling.
    </p>
  </div>
</div>

<style lang="scss">
// Sass variables
$primary-color: #000;
$secondary-color: #6b7280;
$border-radius: 0.5rem;
$spacing: 1.5rem;
$transition: 0.3s ease;

// Mixin for hover effects
@mixin hover-lift {
  transition: transform $transition, box-shadow $transition;
  
  &:hover {
    transform: translateY(-4px);
    box-shadow: 0 10px 25px -5px rgba(0, 0, 0, 0.1);
  }
}

.sass-example {
  padding: $spacing;
  
  .example-card {
    padding: $spacing;
    border-radius: $border-radius;
    background: white;
    border: 1px solid #e5e7eb;
    max-width: 500px;
    margin: 0 auto;
    
    @include hover-lift;
    
    .title {
      font-size: 1.5rem;
      font-weight: 700;
      color: $primary-color;
      margin-bottom: 0.75rem;
    }
    
    .description {
      color: $secondary-color;
      line-height: 1.6;
      font-size: 1rem;
    }
    
    // Nested media query
    @media (max-width: 768px) {
      padding: 1rem;
      
      .title {
        font-size: 1.25rem;
      }
    }
  }
}
</style>

