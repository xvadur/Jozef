---
const pains = [
  { label: "Recepčná má plné ruky", desc: "Pacienti v čakárni, papiere, admin... a ešte telefón" },
  { label: "Zmeškané hovory mimo pracovnej doby", desc: "Pacienti volia po 18:00, cez víkend" },
  { label: "Stratení pacienti = stratené tržby", desc: "Každý zmešknutý hovor je €50–150 menej" },
  { label: "Ďalší zamestnanec to nerieši", desc: "Plat recepčnej je €1,500+/mesiac a problém zostáva" },
];
---
<section class="max-w-7xl mx-auto px-6 pb-12">
  <div class="grid gap-3 md:grid-cols-2 lg:grid-cols-4">
    <div class="lg:col-span-4 p-6"
      style="border: var(--border-brutal); box-shadow: var(--shadow-brutal); background-color: var(--color-silver);">
      <p class="text-xs font-bold uppercase tracking-widest mb-1" style="color: var(--color-blue-deep);">Problém</p>
      <h2 class="text-3xl font-light">Poznáte to?</h2>
    </div>
    {pains.map(p => (
      <div class="p-8" style="border: var(--border-brutal); box-shadow: var(--shadow-brutal); background-color: var(--color-bg);">
        <p class="text-2xl mb-3" aria-hidden="true">✗</p>
        <h3 class="text-base font-semibold mb-2">{p.label}</h3>
        <p class="text-sm font-light" style="opacity: 0.7;">{p.desc}</p>
      </div>
    ))}
    <div class="lg:col-span-4 p-6" style="border: var(--border-brutal); box-shadow: var(--shadow-brutal); background-color: var(--color-text); color: white;">
      <p class="text-sm font-light">
        Zubná klinika zmešká priemerne <strong>10–15% hovorov</strong>.
        Pri 1,200 hovoroch mesačne to znamená <strong style="color: var(--color-blue);">€3,600 stratených – každý mesiac</strong>.
      </p>
    </div>
  </div>
</section>
