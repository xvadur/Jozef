---
import AnimatedBlock from "../ui/AnimatedBlock.tsx";
import BrutalistButton from "../ui/BrutalistButton.astro";

const faqs = [
  { q: "Čo ak AI urobí chybu?", a: "AI má fallback na človeka. Hovory logujeme, aby ste mali prehľad." },
  { q: "Ako je to s GDPR?", a: "Dáta držíme na EU infra. Vieme podpísať DPA." },
  { q: "Koľko trvá setup?", a: "Typicky 1–2 dni + krátke testovanie." },
  { q: "Môžem to zrušiť?", a: "Áno. Mesačný model, bez dlhého viazania." },
];
---
<section class="max-w-7xl mx-auto px-6 pb-12">
  <!-- Demo CTA -->
  <AnimatedBlock client:load delay={0}>
    <div class="p-12 mb-3 text-center block-hover"
      style="border: var(--border-brutal); box-shadow: var(--shadow-brutal); background-color: var(--color-pink);">
      <p class="text-xs font-bold uppercase tracking-widest mb-3 text-white opacity-80">Demo</p>
      <h2 class="text-4xl font-light text-white mb-3">Prestať strácať pacientov.<br />Začať dnes.</h2>
      <p class="text-white font-light mb-6" style="opacity: 0.85;">
        Demo trvá 15 minút. Ukážeme vám, ako by to fungovalo vo vašej prevádzke.
      </p>
      <div class="flex gap-3 justify-center flex-wrap">
        <BrutalistButton href="mailto:adam@xvadur.com" variant="white" size="lg">
          Chcem demo (zadarmo)
        </BrutalistButton>
        <BrutalistButton href="https://cal.com/xvadur" variant="black" size="lg" target="_blank">
          Zavolať / Napísať →
        </BrutalistButton>
      </div>
      <p class="text-white text-sm mt-6" style="opacity: 0.7;">
        ✓ Bez záväzku &nbsp;·&nbsp; ✓ Bez karty &nbsp;·&nbsp; ✓ 15 minút
      </p>
    </div>
  </AnimatedBlock>

  <!-- FAQ -->
  <div class="grid gap-3 md:grid-cols-2">
    <AnimatedBlock client:load delay={0.1} className="md:col-span-2">
      <div class="p-4 block-hover"
        style="border: var(--border-brutal); background-color: var(--color-silver);">
        <p class="text-xs font-bold uppercase tracking-widest" style="color: var(--color-blue-deep);">FAQ</p>
      </div>
    </AnimatedBlock>
    {faqs.map((faq, i) => (
      <AnimatedBlock client:load delay={0.2 + i * 0.08}>
        <div class="p-6 block-hover" style="border: var(--border-brutal); box-shadow: var(--shadow-brutal); background-color: var(--color-bg);">
          <h3 class="text-sm font-bold mb-2">{faq.q}</h3>
          <p class="text-sm font-light" style="opacity: 0.7;">{faq.a}</p>
        </div>
      </AnimatedBlock>
    ))}
  </div>
</section>
